name: Lint Go bridge

on:
  push:
    paths:
      - bridge/tfchain_bridge/**
  workflow_dispatch:

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out code into the Go module directory
        uses: actions/checkout@v4
        with:
          submodules: "true"
          sparse-checkout: |
            clients/tfchain-client-go
            bridge/tfchain_bridge

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.21"
          cache: false
        id: go

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          args: --timeout 3m --verbose
          working-directory: bridge/tfchain_bridge

      - name: staticcheck
        uses: dominikh/staticcheck-action@v1
        with:
          version: "latest"
          # Without this the action installs its own Go, which GOTOOLCHAIN=auto
          # resolves to the newest release -- go1.27.0 today. staticcheck v0.7.0
          # cannot read that toolchain's export data, so the job fails on stdlib
          # packages with no change to this repo. Reuse the Go set up above, as
          # 020_lint_and_test_go_client.yaml already does.
          install-go: false
          working-directory: bridge/tfchain_bridge

      - name: gofmt
        run: |
          cd bridge/tfchain_bridge
          UNFORMATTED=$(gofmt -l .)
          if [ -n "$UNFORMATTED" ]; then
            echo "Files not formatted:"
            echo "$UNFORMATTED"
            gofmt -d .
            exit 1
          fi
